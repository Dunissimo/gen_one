# DAO Венчурного Фонда - Архитектура Smart Contracts

## 📋 Обзор системы

Система управления венчурным фондом на базе DAO с двухуровневым токеном-голосованием и сложным механизмом кворума для разных типов предложений.

---

## 1️⃣ СТРУКТУРА ТОКЕНОВ

### Токен 1: Professional (PROFI) - Системный токен ERC20
```
- Стандарт: ERC20 + ERC20Votes (для делегирования)
- Supply: 100,000 PROFI
- Decimals: 12
- Распределение: равные части междуDAO участниками при инициализации
- Назначение: основа для голосования членов DAO
- Характеристики: 
  - НЕ передаваемый между пользователями
  - Не покупается/продается
  - Привязан только к аккаунту члена DAO
```

### Токен 2: RTKCoin (RTK) - Wrap-токен ERC20
```
- Стандарт: ERC20
- Supply: 20,000,000 RTK
- Decimals: 12
- Стоимость: 1 RTK = 0.5 PROFI (1 ETH = 1 RTK)
- Назначение: делегирование голосов от неучастников
- Характеристики:
  - Покупается за ETH
  - Передаваемый токен
  - Может быть перенаправлен в голос участнику
```

---

## 2️⃣ ТИПЫ ПОЛЬЗОВАТЕЛЕЙ И РОЛИ

### Уровень доступа 1: Неавторизованный пользователь
- Может просматривать все принятые предложения (только результаты)
- Не может взаимодействовать с контрактом
- Доступ только для чтения через события

### Уровень доступа 2: Авторизованный, НЕ участник DAO
- Может купить wrap-токены (RTK)
- Может делегировать свой голос участнику DAO
- Может просматривать активные предложения
- **Не может голосовать напрямую**
- **Не может быть автором предложений**

### Уровень доступа 3: Авторизованный, участник DAO
- Имеет PROFI токены
- Может автоматически голосовать (с делегированием)
- Может создавать предложения (propose)
- Может инициировать голосование
- Может удалять свои же предложения (до голосования)
- Видит все данные участников
- Получает уведомления о результатах голосования

---

## 3️⃣ ТИПЫ ПРЕДЛОЖЕНИЙ (PROPOSE)

| Тип | Код | Описание | Кворум | Параметры |
|-----|------|---------|--------|-----------|
| A | 1 | Инвестирование в новый стартап | Вес голосов | адрес_стартапа, сумма_ETH |
| B | 2 | Доинвестирование в существующий стартап | Вес голосов | адрес_стартапа, сумма_ETH |
| C | 3 | Добавить нового участника DAO | Простое большинство | адрес_пользователя |
| D | 4 | Исключить участника из DAO | Простое большинство | адрес_участника |
| E | 5 | Управление системным токеном (PROFI) | Супербольшинство | параметры_токена |
| F | 6 | Управление wrap-токеном (RTK) | Супербольшинство | параметры_токена |

---

## 4️⃣ МЕХАНИЗМЫ КВОРУМА

### Механизм 1: Простое большинство (50% + 1)
- **Применяется для**: Типы C, D, E, F
- **Расчет**: `(голосов_за > total_votes / 2) AND (голосов_за > голосов_против)`
- **Алгоритм**:
  ```
  quorum_votes = (total_voting_power / 2) + 1
  passed = votes_for >= quorum_votes
  ```

### Механизм 2: Супербольшинство (2/3)
- **Применяется для**: Типы C, D, E, F (альтернатива)
- **Расчет**: `голосов_за >= (total_votes * 2) / 3`
- **Алгоритм**:
  ```
  quorum_votes = (total_voting_power * 2) / 3
  passed = votes_for >= quorum_votes
  ```

### Механизм 3: Голоса по весу (Investment weight voting)
- **Применяется для**: Типы A, B
- **Специфика**: 
  - Голос участника зависит от количества токенов
  - Не требует минимального участия
  - Выигрывает вариант с большей суммой голосов
  
- **Алгоритм**:
  ```
  proposal_passes = votes_for > votes_against
  // Без кворума, просто большинство
  ```

---

## 5️⃣ СТОИМОСТЬ ГОЛОСОВ

### Внутренняя конвертация голосов
```
1 голос участника = 3 PROFI токена

Wrap-токен RTK = 0.5 PROFI
  → 1 RTK = 0.166... голоса (примерно)
  → 6 RTK = 1 голос (более удобно для расчетов)
  
Стоимость в ETH:
  1 RTK = 1 ETH
  → 6 RTK (1 голос) = 6 ETH
  
Для делегирования:
  - Участник DAO имеет N голосов (от PROFI)
  - Неучастник может добавить M голосов (от RTK)
  - Итоговая сила голоса = N + M
```

---

## 6️⃣ УПРАВЛЕНИЕ ПРЕДЛОЖЕНИЯМИ

### Создание предложения (propose)
```solidity
struct Proposal {
    uint256 proposalId;           // Уникальный ID
    address proposer;             // Создатель
    uint8 proposeType;            // Тип A-F (1-6)
    uint256 createdAt;            // Время создания
    ProposalStatus status;        // Статус
    
    // Данные в зависимости от типа
    address targetAddress;        // Для типов A, B, C, D
    uint256 amount;              // Для типов A, B
    bytes proposalData;          // Для типов E, F
    
    // Голосование
    uint256 votingStartTime;     // Когда началось голосование
    uint256 votingEndTime;       // Когда закончится голосование
    uint8 quorumType;            // Какой кворум применить
    
    // Результаты
    uint256 votesFor;
    uint256 votesAgainst;
    uint256 votesAbstain;
    
    // Выполнение
    bool executed;
    ExecutionEvent executionEvent;
}

enum ProposalStatus {
    Pending,      // Ждет голосования
    Active,       // На голосовании
    Canceled,     // Отменено
    Defeated,     // Не прошло голосование
    Succeeded,    // Прошло голосование
    Executed      // Выполнено
}
```

### Удаление предложения (до голосования)
```
Только создатель может удалить
Требуется: вернуть все делегированные токены обратно
Затраты на газ несет создатель
```

---

## 7️⃣ МЕХАНИЗМ ГОЛОСОВАНИЯ

### Инициализация голосования
```solidity
struct Vote {
    uint256 proposalId;
    address voter;
    uint8 support;           // 0=Against, 1=For, 2=Abstain
    uint256 votingPower;     // PROFI + RTK (конвертировано)
    uint256 profiAmount;     // Сколько PROFI использовано
    uint256 rtkAmount;       // Сколько RTK делегировано
    uint256 votedAt;
}
```

### Распределение голосов
```
Голосует только участник DAO (владелец PROFI)

1. Базовый голос от PROFI:
   voting_power = profi_balance / 3
   
2. Добавленные голоса от RTK (если делегированы):
   delegated_power = rtk_delegated / 6
   
3. Итоговая сила голоса:
   total_voting_power = voting_power + delegated_power
```

---

## 8️⃣ ДЕЛЕГИРОВАНИЕ ГОЛОСОВ

### Процесс делегирования (для неучастников)
```
1. Неучастник покупает RTK токены (за ETH)
   - Отправляет 1 ETH → получает 1 RTK
   - Хранит RTK в своем адресе

2. Неучастник делегирует свой голос
   - Выбирает участника DAO (адрес)
   - Переводит RTK в смарт-контракт голосования
   - Смарт-контракт:
     a. Блокирует RTK до конца голосования
     b. Добавляет голоса к выбранному участнику
     c. Записывает делегирование в историю

3. После голосования
   - Неучастник может забрать свой RTK обратно
   - Или делегировать новому участнику
   - Участник DAO видит кумулятивный голос
```

### Актуальные делегирования
```
Структура хранения:
mapping(address delegator => mapping(address delegatee => uint256 rtk_amount))

Смарт-контракт должен отслеживать:
- Кто от кого делегировал голосы
- Сколько RTK в общей сложности привлечено
- Все активные делегирования
```

---

## 9️⃣ ИНИЦИАЛИЗАЦИЯ СИСТЕМЫ

### При развертывании контрактов
```
1. Развернуть токены:
   - Professional (PROFI): 100,000 с 12 decimals
   - RTKCoin (RTK): 20,000,000 с 12 decimals

2. Создать аккаунты:
   ┌─────────────┬─────────┬────────────────┬──────────┐
   │ Имя         │ Статус  │ PROFI Balance  │ ETH      │
   ├─────────────┼─────────┼────────────────┼──────────┤
   │ Tom         │ DAO     │ 25,000         │ 10 ETH   │
   │ Ben         │ DAO     │ 25,000         │ 10 ETH   │
   │ Rick        │ DAO     │ 25,000         │ 10 ETH   │
   │ Jack        │ Non-DAO │ 0              │ 20 ETH   │
   │ Startup A   │ Non-DAO │ 0              │ 5 ETH    │
   │ Fond (Fund) │ Vault   │ 25,000         │ 1,000,000│
   └─────────────┴─────────┴────────────────┴──────────┘

3. Инициализировать Governor контракт:
   - Установить параметры голосования
   - Связать с PROFI токеном (ERC20Votes)
   - Установить временные параметры
```

---

## 🔟 ДАННЫЕ, КОТОРЫЕ ХРАНИТ СИСТЕМА

### На блокчейне (состояние контракта):

1. **Участники DAO**
   ```solidity
   mapping(address => bool) isDAOMember;
   address[] daoMembers;
   ```

2. **Предложения**
   ```solidity
   mapping(uint256 => Proposal) proposals;
   uint256[] proposalIds;
   ```

3. **Голоса по предложениям**
   ```solidity
   mapping(uint256 proposalId => mapping(address => Vote)) votes;
   ```

4. **Делегирования (история)**
   ```solidity
   mapping(address => Delegation[]) delegationHistory;
   struct Delegation {
       uint256 proposalId;
       address delegatee;
       uint256 rtkAmount;
       uint256 timestamp;
   }
   ```

5. **Инвестиции в стартапы**
   ```solidity
   mapping(address => uint256) startupInvestments;
   mapping(address => uint256[]) startupProposalHistory;
   ```

---

## 1️⃣1️⃣ СОБЫТИЯ ДЛЯ УВЕДОМЛЕНИЙ

```solidity
// Управление предложениями
event ProposalCreated(
    uint256 indexed proposalId,
    address indexed proposer,
    uint8 proposeType
);

event ProposalCanceled(
    uint256 indexed proposalId,
    address indexed canceledBy
);

event VotingStarted(
    uint256 indexed proposalId,
    uint256 startTime,
    uint256 endTime
);

// Голосование
event VoteCast(
    uint256 indexed proposalId,
    address indexed voter,
    uint8 support,
    uint256 votingPower
);

event DelegationCreated(
    uint256 indexed proposalId,
    address indexed delegator,
    address indexed delegatee,
    uint256 rtkAmount
);

// Результаты
event ProposalPassed(
    uint256 indexed proposalId,
    uint256 votesFor,
    uint256 votesAgainst
);

event ProposalFailed(
    uint256 indexed proposalId,
    uint256 votesFor,
    uint256 votesAgainst
);

// Исполнение
event ProposalExecuted(
    uint256 indexed proposalId,
    uint8 eventType
);

event MemberAdded(address indexed newMember);
event MemberRemoved(address indexed member);
```

---

## 1️⃣2️⃣ ПРИОРИТЕТ ПРЕДЛОЖЕНИЙ (PRIORITY LEVEL)

```
Используется для:
- Упорядочивания в очереди
- Определения порядка выполнения
- Уведомления пользователей

Приоритеты:
1 = Low     (Управление токенами)
2 = Medium  (Добавить/исключить участника)
3 = High    (Инвестиции в новый стартап)
4 = Critical (Экстренные изменения)
```

---

## 1️⃣3️⃣ ВРЕМЕННЫЕ ПАРАМЕТРЫ

```solidity
// Глобальные параметры
uint256 votingDelay = 1 block;        // Задержка перед началом голосования
uint256 votingPeriod = 50,400 blocks; // ~7 дней (для тестнета: короче)
uint256 proposalThreshold = 1 PROFI;  // Минимум для создания proposal

// Для инвестиций (типы A, B)
uint256 investmentVotingPeriod = 100,800 blocks; // ~14 дней

// Для управления (типы C, D, E, F)
uint256 governanceVotingPeriod = 50,400 blocks;  // ~7 дней
```

---

## 1️⃣4️⃣ ВНУТРЕННЯЯ ЛОГИКА РАСЧЕТОВ

### Расчет голосов участника

```javascript
// Базовый голос от PROFI
const baseVotingPower = profiBalance / 3;

// Голосов от делегированных RTK
const delegatedVotes = delegatedRtkAmount / 6;

// Итоговая сила голоса
const totalVotingPower = baseVotingPower + delegatedVotes;
```

### Проверка кворума для типов A, B

```javascript
// Механизм: Голоса по весу
const votesFor = proposal.votesFor;
const votesAgainst = proposal.votesAgainst;

// Просто большинство голосов, без минимума участников
const passed = votesFor > votesAgainst;
```

### Проверка кворума для типов C, D, E, F

```javascript
// Механизм 1: Простое большинство (50% + 1)
const totalVotingPower = proposal.votesFor + proposal.votesAgainst;
const quorumRequired = Math.floor(totalVotingPower / 2) + 1;
const passed = proposal.votesFor >= quorumRequired;

// Механизм 2: Супербольшинство (2/3)
const quorumRequired = Math.ceil((totalVotingPower * 2) / 3);
const passed = proposal.votesFor >= quorumRequired;
```

---

## 1️⃣5️⃣ ЖИЗНЕННЫЙ ЦИКЛ ПРЕДЛОЖЕНИЯ

```
┌─────────────────────────────────────────────────────────────┐
│                    ЖИЗНЕННЫЙ ЦИКЛ PROPOSE                   │
└─────────────────────────────────────────────────────────────┘

1. СОЗДАНИЕ (Pending)
   ↓ Участник DAO создает propose
   ├─ Проверка: только участники могут создавать
   ├─ Параметры: тип, данные (адрес, сумма, параметры)
   ├─ Сохранение в хранилище
   └─ Эвент: ProposalCreated

2. ОПЦИЯ: ОТМЕНА (Canceled)
   ↓ До начала голосования
   ├─ Проверка: только создатель может отменить
   ├─ Возврат делегированных токенов
   └─ Эвент: ProposalCanceled

3. ИНИЦИИРОВАНИЕ ГОЛОСОВАНИЯ (Active)
   ↓ Участник инициирует голосование (может быть не создатель)
   ├─ Проверка: proposal статус = Pending
   ├─ Выбор кворума (в зависимости от типа)
   ├─ Установка времени начала/конца
   └─ Эвент: VotingStarted

4. ГОЛОСОВАНИЕ (Active)
   ↓ Участники голосуют
   ├─ Неучастники могут делегировать голоса
   ├─ Запись голосов в систему
   ├─ Эвент: VoteCast
   └─ Эвент: DelegationCreated (если есть делегирование)

5. ЗАВЕРШЕНИЕ ГОЛОСОВАНИЯ (Succeeded/Defeated)
   ↓ После истечения votingPeriod
   ├─ Расчет голосов в зависимости от типа и кворума
   ├─ Если passed = true → Succeeded
   ├─ Если passed = false → Defeated
   └─ Эвент: ProposalPassed/ProposalFailed

6. ИСПОЛНЕНИЕ (Executed)
   ↓ Если proposal Succeeded
   ├─ Выполнение соответствующего события:
   │  ├─ Тип A: Перевести ETH стартапу A
   │  ├─ Тип B: Добавить инвестицию в стартап B
   │  ├─ Тип C: Добавить нового участника
   │  ├─ Тип D: Исключить участника
   │  ├─ Тип E: Изменить параметры PROFI
   │  └─ Тип F: Изменить параметры RTK
   ├─ Освобождение заблокированных токенов (RTK)
   ├─ Очистка временных данных
   └─ Эвент: ProposalExecuted
```

---

## 1️⃣6️⃣ ОБРАБОТКА ОШИБОК И ОГРАНИЧЕНИЯ

```solidity
// Проверки при создании proposal
require(isDAOMember[msg.sender], "Only DAO members can propose");
require(amount > 0 || targetAddress != address(0), "Invalid proposal data");

// Проверки при голосовании
require(proposal.status == ProposalStatus.Active, "Voting not active");
require(getVotes(msg.sender) > 0, "No voting power");
require(votes[proposalId][msg.sender].voter == address(0), "Already voted");

// Проверки при делегировании
require(rtkToken.balanceOf(msg.sender) >= amount, "Insufficient RTK");
require(isDAOMember[delegatee], "Delegatee must be DAO member");

// Проверки при исполнении
require(proposal.votingEndTime <= block.timestamp, "Voting still active");
require(proposal.status == ProposalStatus.Succeeded, "Proposal did not pass");
require(!proposal.executed, "Already executed");
```

---

## 1️⃣7️⃣ СМАРТ-КОНТРАКТЫ (ФАЙЛОВАЯ СТРУКТУРА)

```
contracts/
├── Professional.sol         # ERC20 + ERC20Votes токен PROFI
├── RTKCoin.sol              # ERC20 токен RTK
├── FundGovernor.sol         # Основной Governor контракт
├── FundVault.sol            # Хранилище средств (казна фонда)
├── VotingSystem.sol         # Механика голосования и кворума
├── ProposalManager.sol      # Управление предложениями
├── Interfaces/
│   ├── IFundGovernor.sol
│   ├── IProposal.sol
│   └── IVoting.sol
└── test/
    ├── unit/
    ├── integration/
    └── mocks/
```

---

## 1️⃣8️⃣ ВАЖНЫЕ ЗАМЕЧАНИЯ

### ⚠️ Осторожно с типизацией

1. **Wrap-токены RTK:**
   - Это **не** долговые расписки
   - Это механизм **временного** делегирования голосов
   - После голосования RTK должны быть **возвращены** владельцу
   - **НЕ** работают как пула акций в стартапах

2. **PROFI токены:**
   - **НЕЛЬЗЯ** передавать между пользователями
   - **НЕЛЬЗЯ** покупать/продавать
   - Зафиксированы в аккаунте участника
   - Это **только** инструмент голосования

3. **Инвестиции:**
   - Хранятся в FundVault (смарт-контракт)
   - Выплачиваются ETH-ом (не токенами)
   - Ведется история по каждому стартапу

### ⚠️ Безопасность

1. **Проверка Reentrancy:**
   - При передаче ETH использовать `call` с проверкой
   - При возврате RTK использовать SafeTransfer

2. **Overflow/Underflow:**
   - Solidity 0.8+ автоматически проверяет
   - Но проверьте вычисления кворума (деления!)

3. **Двойное голосование:**
   - Невозможно голосовать дважды за одно proposal
   - Невозможно делегировать один RTK дважды

---

## 1️⃣9️⃣ ТЕСТИРОВАНИЕ СЦЕНАРИЕВ

1. **Сценарий 1: Инвестирование в новый стартап (Тип A)**
   - Tom создает proposal (тип A, адрес Startup A, 100 ETH)
   - Инициирует голосование с кворумом "Вес голосов"
   - Jack покупает 6 RTK (1 голос) и делегирует Ben
   - Tom голосует "За" (33 голоса от PROFI)
   - Ben голосует "За" (33 + 1 = 34 голоса)
   - Итого: За = 67, Против = 0 → Passed
   - Контракт отправляет 100 ETH на адрес Startup A

2. **Сценарий 2: Добавить участника (Тип C)**
   - Rick создает proposal (тип C, адрес Jack)
   - Инициирует голосование с кворумом "Простое большинство"
   - Голосуют: Tom (За), Ben (За), Rick (Против)
   - Итого голосов: 66 (за) vs 33 (против)
   - Кворум: (66 + 33) / 2 + 1 = 50
   - 66 >= 50 → Passed
   - Jack добавлен в DAO члены

---

## 🎯 ЗАКЛЮЧЕНИЕ

Эта архитектура обеспечивает:
- ✅ Прозрачное управление фондом через DAO
- ✅ Гибкую систему голосования с разными кворумами
- ✅ Механизм делегирования для расширения участия
- ✅ Хранение всех решений в блокчейне
- ✅ Безопасность через контролируемое выполнение (execute)
- ✅ Отслеживание инвестиций и результатов голосования
